# Dockerfile combiné - Utilise l'image officielle Papercups
# Phoenix sert déjà le frontend et le backend sur le même port
FROM papercups/papercups:latest

# Variables d'environnement
ENV MIX_ENV=prod
ENV PORT=4000

# Exposer le port
EXPOSE 4000

# Script de démarrage
CMD ["/bin/sh", "-c", "sleep 5 && /entrypoint.sh db createdb && /entrypoint.sh db migrate && /entrypoint.sh run"]
